require 'spec_helper'

describe Flexible::Retry do
  it 'has a version number' do
    expect(Flexible::Retry::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
